name: Deploy

on:
  push:
    branches: [main]

jobs:
  ci:
    uses: wcmchenry3-stack/buffingchi_site/.github/workflows/ci.yml@main
    secrets: inherit

  deploy:
    needs: ci
    uses: wcmchenry3-stack/.github/.github/workflows/called-deploy-render.yml@main
    with:
      service-name: 'buffingchi'
      service-id: 'srv-d74mc52dbo4c738vqkk0'
      zap-target-url: 'https://buffingchi.com'
    secrets: inherit
